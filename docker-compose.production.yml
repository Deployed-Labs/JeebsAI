services:
  jeebs:
    image: ghcr.io/deployed-labs/jeebsai:latest
    restart: always
    env_file:
      - /etc/jeebs.env
    volumes:
      - jeebs-data:/var/lib/jeebs
    ports:
      - "127.0.0.1:8080:8080"

volumes:
  jeebs-data:
