## Example environment file for Jeebs
# Copy to /etc/jeebs.env and edit values before starting the service

DATABASE_URL=sqlite:/var/lib/jeebs/jeebs.db
PORT=8080
# Optional logging level
RUST_LOG=info

# Example: if you have SMTP/email settings or API keys, add them here
# SMTP_HOST=smtp.example.com
# SMTP_USER=jeebs@example.com
# SMTP_PASS=secret
